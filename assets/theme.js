// Alternador de Tema Claro / Escuro — DESATIVADO a pedido do cliente.
// O site QRV Artigos Táticos ficou fixo no tema claro padrão. O botão
// (.theme-toggle) foi removido do topbar de todas as páginas, o script
// inline que lia data-theme do localStorage no <head> foi removido, e a
// tag <script> que carregava este arquivo também foi removida de todas
// as páginas. Este arquivo foi esvaziado (em vez de apagado) só por
// segurança, caso algo ainda referencie o caminho assets/theme.js — ele
// não faz mais nada.
